/** MaNaReD interactive roles — Figma `MaNaReD.colour.interactive.*` and provenance text. */

/** Active filter chip shell — Figma `active-chip` / `interactive/chip/active`. */
export const INTERACTIVE_ACTIVE_CHIP =
  "inline-flex h-6 shrink-0 items-center gap-2 overflow-hidden rounded-lg border border-solid border-border-secondary bg-chip-active px-[length:var(--spacing-2)] py-[length:var(--spacing-1)] text-3xs font-normal leading-4 tracking-[-0.12px] text-secondary hover:bg-chip-hover";

/** Chip remove control — inherits chip text colour. */
export const INTERACTIVE_CHIP_REMOVE =
  "flex shrink-0 items-center justify-center text-secondary hover:text-primary";

/** Provenance helper copy — Figma `colour/text/tertiary`, italic. */
export const INTERACTIVE_PROVENANCE_TEXT = "ml-2 italic text-tertiary";

/** Secondary chip-bar control (More filters, Sort) — page primary + secondary border/text. */
export const INTERACTIVE_FILTER_CONTROL = "text-2xs";
