/** MaNaReD gradient surface roles — Figma `colour/gradient/*` paint styles. */

/** Chip bar band — Figma `colour/gradient/light|dark/chip-bar` (349:3993). */
export const GRADIENT_CHIP_BAR = "surface-gradient-chip-bar";

/** Primary nav sidebar shell — Figma `colour/gradient/light|dark/side-bar`. */
export const GRADIENT_SIDEBAR = "surface-gradient-sidebar";

/** Fixed 192px nav rail width — Figma `--layout/sidebar/width` (339:3237). */
export const NAV_SIDEBAR_SHELL = "nav-sidebar-shell";

/** Collapsed 56px nav rail — Figma `nav-side-bar` state=collapsed (339:3238). */
export const NAV_SIDEBAR_SHELL_COLLAPSED = "nav-sidebar-shell-collapsed";

/** Filter sidebar scroll region — Figma `colour/gradient/light|dark/filter`. */
export const GRADIENT_FILTER = "surface-gradient-filter";

/** Search dropdown row hover — Figma `colour/gradient/light/dropdownSelect`; applied via `INTERACTIVE_SEARCH_DROPDOWN_ROW`. */
export const GRADIENT_DROPDOWN_SELECT = "surface-gradient-filter";

/** Contextual header band — Figma `colour/gradient/light|dark/context-bar`. */
export const GRADIENT_CONTEXT_BAR = "surface-gradient-context-bar";
